# Asset manifest

Application code references stable asset keys through `src/lib/assets.ts`. Replaceable Figma exports, backgrounds, characters, avatars, mascot art, and audio can be added here later without coupling business logic to filenames. Phase 1 intentionally uses a generic placeholder.
