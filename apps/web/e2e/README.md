# Playwright E2E skeleton

Critical product-flow tests will be added after authentication and onboarding begin in Phase 2. The root `playwright.config.ts` already defines the web server, base URL, Chromium project, and report output.
